import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ['images.unsplash.com','scimplify.com','www.scimplify.com','cdn.scimplify.com'],
  },
};

export default nextConfig;
